# Agent 2 Memory

Agent 2 is the delivery department.

Memory layers:

- `short-term/` for immediate execution notes
- `mid-term/` for current task and rolling implementation summary
- `long-term/` for stable implementation knowledge

Legacy folders:

- `daily/` can be treated as old short-term notes
- `summaries/` can be treated as old mid-term notes
