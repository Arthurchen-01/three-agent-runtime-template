# Agent 3 Memory

Agent 3 is the quality and audit department.

Memory layers:

- `short-term/` for immediate audit notes
- `mid-term/` for current review cycle summaries
- `long-term/` for stable review and risk knowledge

Legacy folders:

- `daily/` can be treated as old short-term notes
- `summaries/` can be treated as old mid-term notes
