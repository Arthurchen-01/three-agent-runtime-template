# Example Smoke Test Requirement

Please create `30_execution/hello.txt` with the text `Hello from Agent 2`.

Use this only as a reference.
The active request should normally be written to `00_input/requirement.md`.
